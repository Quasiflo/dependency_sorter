name: example

dependencies:
  shared_ui:
    git:
      url: https://example.com/shared_ui.git
      # Pinned for stability.
      ref: abc123
  yaml: ^3.0.0
  'quoted_pkg': ^1.0.0
  flutter:
    sdk: flutter
  args: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
