---
name: dependency_sorter
description: Sort your pubspec dependencies alphabetically. A tiny, well-tested CLI that fixes what the `sort_pub_dependencies` lint reports.
version: 0.2.0
maintainer: Luke Aguilar (@Attempt3035)
repository: https://github.com/Quasiflo/dependency_sorter
homepage: https://github.com/Quasiflo/dependency_sorter
issue_tracker: https://github.com/Quasiflo/dependency_sorter/issues

environment:
  sdk: ^3.10.0

executables:
  dependency_sorter:

dependencies:
  args: ^2.4.2
  meta: ^1.19.0
  path: ^1.9.1
  yaml: ^3.1.2

dev_dependencies:
  quasiflo_lints: ^0.1.1
  test: ^1.25.6

topics:
  - pubspec
  - dependencies
  - formatter
  - cli
